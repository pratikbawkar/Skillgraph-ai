name: CI

on:
  pull_request:
    branches: [develop]
  push:
    branches: [develop]

jobs:
  backend-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements-dev.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip setuptools
          cd backend
          pip install --upgrade --only-binary :all: pydantic-core
          pip install -r requirements.txt
          pip install pytest pytest-cov httpx ruff mypy bandit pip-audit

      - name: Lint backend with ruff
        run: |
          cd backend
          ruff check app tests

      - name: Type check backend
        run: |
          cd backend
          mypy app --ignore-missing-imports --no-error-summary

      - name: Security check backend (bandit)
        run: |
          cd backend
          bandit -r app --severity-level=medium

      - name: Dependency audit backend
        run: |
          cd backend
          pip-audit --desc

      - name: Run backend tests with coverage
        run: |
          cd backend
          pytest --cov=app --cov-report=xml --cov-report=term --cov-fail-under=80 tests/

      - name: Upload backend coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/coverage.xml
          flags: backend
          name: backend
          fail_ci_if_error: true

  frontend-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Lint frontend
        run: |
          cd frontend
          npm run lint

      - name: Type check frontend
        run: |
          cd frontend
          npx tsc --noEmit

      - name: Run frontend tests with coverage
        run: |
          cd frontend
          npm run test -- --run --coverage

      - name: Security audit frontend dependencies
        run: |
          cd frontend
          npm audit --audit-level=moderate

      - name: Upload frontend coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./frontend/coverage/coverage-final.json
          flags: frontend
          name: frontend
          fail_ci_if_error: true
